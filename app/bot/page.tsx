import { prisma } from '@/lib/prisma';
import { Card } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
export default async function BotPage() { const features = await prisma.botFeature.findMany(); return <div className="container-page section"><h1 className="text-4xl font-bold">Telegram-бот-ассистент</h1><p className="mt-4 max-w-3xl text-slate-600">Образовательный помощник: напоминает об уроках, помогает закрепить привычки и присылает краткие рыночные обзоры без обещаний прибыли.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{features.map((f)=><Card key={f.id} className="p-6"><h3 className="font-semibold">{f.title}</h3><p className="mt-2 text-slate-600">{f.description}</p></Card>)}</div><Card className="mt-8 p-6"><h2 className="text-2xl font-bold">Demo webhook section</h2><p className="mt-2 text-slate-600">/mock/bot/webhook → учебные напоминания, мини-дайджесты и разбор вопросов пользователя.</p><Button className="mt-4">Подключить бота</Button></Card></div>; }
